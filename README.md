# Beanster Android App OCR Acceptance

This repository is used to run APK-level Android acceptance tests for Beanster Sips.

Pass condition: install the APK in Android, load the user's original cup photo through the app test path, and the app must return `桂花米酿拿铁`.
